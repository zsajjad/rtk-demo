// import { selectHackerNewsArticlesDomain } from '../selectors';

describe('selectHackerNewsArticlesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
